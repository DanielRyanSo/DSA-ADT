// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#define MAX 10;

typedef struct{
    int items[MAX];
    int top;
}Stack;

Stack* initialize();
void push(Stack* s, int value);
int pop(Stack* s);
int peek(Stack* s);
void display(Stack* s);

int main() {
    Stack* S = initialize();
    return 0;
}

Stack* initialize(){
    Stack *S = malloc 
}
int pop(Stack* s);
int peek(Stack* s);
void display(Stack* s);

